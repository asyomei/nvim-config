return {
  "ray-x/lsp_signature.nvim",
  opts = {
    hint_enable = false,
    handler_opts = {
      border = "none",
    },
    always_trigger = false,
    extra_triggers_chars = {"(", ","},
  },
}
